const add = (a, b) => {
  return a + b;
};

const sub = (a, b) => {
  return a - b;
};

const mul = (a, b) => {
  return a * b;
};

const div = (a, b) => {
  return a / b;
};

const name = "Beta";

// module.exports.add = add;
// module.exports.sub = sub;
// module.exports.multi = mul;
// module.exports.divi = div;
// module.exports.name = name;

module.exports = { add, sub, mul, div, name };
